# ============================================================================
# DATABASE CONFIGURATION (Priority Order)
# ============================================================================
# 1. Generic Postgres (PRIMARY for Production)
DATABASE_URL=postgres://user:password@host:port/dbname?sslmode=require

# 2. Supabase (Alternative Production/Dev)
# Used if DATABASE_URL is not set AND these are present
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# 3. SQLite (LOCAL DEVELOPMENT ONLY)
# Set to 'true' to use local info.sqlite file. 
# STRICTLY FORBIDDEN IN PRODUCTION (will warn/fail check-env)
USE_SQLITE=false

# ============================================================================
# STORAGE CONFIGURATION (AWS S3)
# ============================================================================
# Required for storing Resumes, PDFs, and Cover Letters in Production
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key
AWS_REGION=us-east-1
AWS_S3_BUCKET=your-bucket-name

# ============================================================================
# AI PROVIDER CONFIGURATION (OpenRouter-First Architecture)
# ============================================================================
# Priority: OPENROUTER (Primary Key) → OPENROUTER (Fallback Key) → OLLAMA
# Fallback key is used when primary key fails (billing, rate limit, etc.)

# OpenRouter Configuration (PRIMARY - Cloud AI)
OPENROUTER_API_KEY=your-openrouter-api-key
OPENROUTER_API_FALLBACK_KEY=your-backup-openrouter-api-key  # Optional: backup key
OPENROUTER_MODEL=google/gemini-2.0-flash-001
OPENROUTER_FALLBACK_MODEL=meta-llama/llama-3.1-8b-instruct:free
OPENROUTER_MAX_CALLS_PER_DAY=100

# Ollama Configuration (FALLBACK - Local AI)
# Only used when OpenRouter fails
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=phi3:mini
OLLAMA_FALLBACK_MODEL=qwen2.5:3b

# Job Source API Keys (Get these from respective providers)
# See README.md for instructions on how to obtain keys.
# Adzuna (developer.adzuna.com)
ADZUNA_APP_ID=
ADZUNA_API_KEY=

# ZipRecruiter (www.ziprecruiter.com/publishers/program-overview)
ZIPRECRUITER_KEY=

# Jooble (jooble.org/api/about)
JOOBLE_KEY=

# Careerjet (careerjet.com/partners/api/)
CAREERJET_KEY=

# USAJOBS (developer.usajobs.gov)
USAJOBS_API_KEY=

# RapidAPI (optional aggregator)
RAPIDAPI_KEY=

# SerpAPI (Google Jobs)
SERPAPI_KEY=

# Vercel Configuration (Optional)
VERCEL_TOKEN=
