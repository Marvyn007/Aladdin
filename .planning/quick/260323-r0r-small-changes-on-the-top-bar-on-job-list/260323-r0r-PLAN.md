---
phase: quick-260323-r0r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/JobList.tsx
autonomous: true
requirements:
  - PH1-FR1
  - PH1-FR5
must_haves:
  truths:
    - "Fresh/saved/archived tabs render as crisp blue pills so the selector stands out and feels clickable."
    - "Saved view only shows jobs whose status is saved, even when filters or search mode would normally show other items."
    - "Fresh shows jobs posted within the last 30 days while Archived surfaces jobs posted more than 30 days ago."
  artifacts:
    - path: "src/components/layout/JobList.tsx"
      provides: "The job list controls + cards (status tabs, save button, job filters)."
      min_lines: 200
  key_links:
    - from: "src/components/layout/JobList.tsx"
      to: "src/store/useStore.ts"
      via: "jobStatus state, job array, and pagination metadata"
      pattern: "useStore(state => state.jobStatus)"
    - from: "src/components/layout/JobList.tsx"
      to: "src/store/useStore.ts"
      via: "search mode helpers + jobs list"
      pattern: "useStore(state => state.searchResults)"
---
<objective>
Keep the job list tabs, save icon, and filtering logic aligned with the new UX direction so the selector feels blue, the bookmark icon loses its box, and each tab only shows the jobs it promises.
Purpose: Protect the dashboard from stale filters while introducing the 30-day fresh/archived split and a blue-focused tab treatment that matches the design system.
Output: Updated `JobList.tsx` with accent-colored tabs, a borderless save icon, and a status-aware filter pipeline that honors saved/fresh/archived semantics.
</objective>

<execution_context>
@C:/Users/iamma/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/iamma/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/components/layout/JobList.tsx
@src/store/useStore.ts
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh status tabs and save icon styling</name>
  <files>src/components/layout/JobList.tsx</files>
  <action>Redefine `getStatusTabStyle` so the pills use `var(--accent)` for the background and text (use `var(--accent-muted)` for the unselected glow) and keep the container padding/rounding consistent with the design system defined in `globals.css`. While here, simplify the save button so it loses its border/box (transparent background, no outline, minimal padding) but still shows the bookmark icon in `var(--accent)` when saved and `var(--text-tertiary)` when not.</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Status tabs are accent-blue pills with the active tab clearly highlighted, and the save button is icon-only (no surrounding box) while remaining accessible.</done>
</task>

<task type="auto">
  <name>Task 2: Filter jobs by status/time</name>
  <files>src/components/layout/JobList.tsx</files>
  <action>Introduce helpers to derive the posted timestamp (falling back from `original_posted_date`, `posted_at`, then `fetched_at`) and a 30-day threshold. After evaluating `searchMode ? searchResults : filteredJobs`, run the array through a `matchesStatusFilter(job, jobStatus)` predicate that returns `job.status === 'saved'` for saved, `within 30 days` for fresh, and `older than 30 days` for archived. Use the resulting array for both search and filtered renders so pagination and cards never show the wrong bucket even if the backend returns mixed ages.</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>The Fresh tab renders only jobs posted within thirty days, the Archived tab renders only jobs posted earlier than thirty days, and the Saved tab always renders the saved jobs regardless of other filters.</done>
</task>

</tasks>

<verification>
Automated: `npm test` (run before edits to capture baseline and again after).
Manual: Start the dev server, log in, open the job list, and toggle each tab to confirm the pills are blue, Saved only shows saved cards, Fresh shows recent jobs, Archived shows older jobs, and the save/bookmark icon no longer has a surrounding box.
</verification>

<success_criteria>
- Status selector pills are blue and readable on the top bar.
- Save/bookmark button is borderless yet still signals saved vs unsaved.
- Each tab filters the job cards according to the saved flag or the 30-day recency threshold.
- `npm test` passes after the UI changes.
</success_criteria>

<output>
After completion, create `.planning/quick/260323-r0r-small-changes-on-the-top-bar-on-job-list/260323-r0r-SUMMARY.md`.
</output>
