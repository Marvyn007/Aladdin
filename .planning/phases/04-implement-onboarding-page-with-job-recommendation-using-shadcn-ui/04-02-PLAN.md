---
phase: 04-implement-onboarding-page-with-job-recommendation-using-shadcn-ui
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/app/onboarding/layout.tsx
  - src/app/onboarding/page.tsx
  - src/components/onboarding/OnboardingWizard.tsx
  - src/components/onboarding/StepOne.tsx
  - src/components/onboarding/StepTwo.tsx
  - src/components/onboarding/QuestionMultiSelect.tsx
  - src/components/onboarding/QuestionSingleSelect.tsx
  - src/components/onboarding/QuestionFileUpload.tsx
  - src/components/onboarding/QuestionText.tsx
autonomous: false
requirements:
  - D-01
  - D-02
  - D-03
  - D-08
  - D-10
  - D-11
  - D-12
  - D-13
  - D-16
  - D-17
  - D-19
  - D-20
  - D-21
  - D-22

must_haves:
  truths:
    - "User can visit /onboarding and see a multi-step wizard with progress bar"
    - "User can answer questions on step 1 (Q1-5) and click Next to go to step 2"
    - "User can go Back from step 2 to step 1 without losing answers"
    - "User can upload a PDF resume and see the filename displayed"
    - "User can upload a LinkedIn PDF with an info tooltip showing 5-step download instructions"
    - "User can click Complete on step 2 to save and redirect to /"
    - "Onboarding page uses dark themed shadcn UI scoped by tweakcn theme"
  artifacts:
    - path: "src/app/onboarding/layout.tsx"
      provides: "Scoped theme wrapper with dark class"
      contains: "onboarding-theme"
    - path: "src/app/onboarding/page.tsx"
      provides: "Server component entry point"
      contains: "OnboardingWizard"
    - path: "src/components/onboarding/OnboardingWizard.tsx"
      provides: "Client wizard with step state machine"
      contains: "'use client'"
    - path: "src/components/onboarding/QuestionFileUpload.tsx"
      provides: "File upload component for resume + linkedin"
      contains: "accept=\"application/pdf\""
  key_links:
    - from: "src/components/onboarding/OnboardingWizard.tsx"
      to: "/api/onboarding"
      via: "fetch POST on handleNext and handleComplete"
      pattern: "fetch.*api/onboarding"
    - from: "src/components/onboarding/QuestionFileUpload.tsx"
      to: "/api/upload-resume"
      via: "FormData POST for resume file upload"
      pattern: "fetch.*api/upload-resume"
    - from: "src/components/onboarding/QuestionFileUpload.tsx"
      to: "/api/upload-linkedin"
      via: "FormData POST for LinkedIn file upload"
      pattern: "fetch.*api/upload-linkedin"
    - from: "src/app/onboarding/layout.tsx"
      to: "src/app/onboarding/theme.css"
      via: "CSS import"
      pattern: "import.*theme\\.css"
---

<objective>
Build the full /onboarding wizard UI as a multi-step form using shadcn/ui components with the scoped tweakcn dark theme.

Purpose: This is the primary user-facing onboarding experience. Users walk through 2 steps of questions, upload files, and submit to save preferences.
Output: Complete /onboarding page with wizard, file uploads, navigation, and API persistence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-implement-onboarding-page-with-job-recommendation-using-shadcn-ui/04-CONTEXT.md
@.planning/phases/04-implement-onboarding-page-with-job-recommendation-using-shadcn-ui/04-RESEARCH.md
@.planning/phases/04-implement-onboarding-page-with-job-recommendation-using-shadcn-ui/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs + existing codebase -->

From src/lib/onboarding.ts (after Plan 01):
```typescript
export type OnboardingStepId = 1 | 2;
export interface OnboardingQuestion {
  key: string; step: OnboardingStepId; order: number;
  title: string; description: string; type: OnboardingQuestionType;
  required: boolean; rationale: string; options?: readonly OnboardingOption[];
  placeholder?: string; helperText?: string;
}
export interface OnboardingAnswerRecord {
  questionKey: string; step: OnboardingStepId; order: number;
  type: OnboardingQuestionType; title: string; value: unknown;
  answerText: string | null; updatedAt?: string | null;
}
export const ONBOARDING_QUESTIONS: readonly OnboardingQuestion[]; // 11 questions
export function getOnboardingQuestionsByStep(step: OnboardingStepId): OnboardingQuestion[];
export const ONBOARDING_STEP_META: Record<OnboardingStepId, { title: string; subtitle: string }>;
```

From src/lib/onboarding-db.ts:
```typescript
export interface OnboardingSnapshot {
  state: OnboardingStateRecord;
  answers: OnboardingAnswerRecord[];
  answersByKey: Record<string, OnboardingAnswerRecord>;
  requiredAnswered: number; requiredTotal: number; progress: number; completed: boolean;
}
```

From src/app/api/onboarding/route.ts:
```
GET /api/onboarding -> OnboardingSnapshot
POST /api/onboarding { currentStep: number, complete?: boolean, answers: [{questionKey, value}] } -> { success: true, ...OnboardingSnapshot }
```

From src/app/api/upload-resume/route.ts:
```
POST /api/upload-resume (FormData with 'file' field) -> { resume: { id, filename, ... } }
```

From src/app/api/upload-linkedin/route.ts (ALREADY EXISTS in codebase):
```
POST /api/upload-linkedin (FormData with 'file' field) -> { success: true, profile: { id, filename, parsed_json } }
```
NOTE: This route already exists at src/app/api/upload-linkedin/route.ts. It accepts PDF files, validates the extension, stores via insertLinkedInProfile, and returns { success, profile }. The response shape differs from upload-resume (uses `profile` not `resume`).

shadcn components available (from Plan 01):
- @/components/ui/progress (Progress)
- @/components/ui/card (Card, CardContent, CardHeader, CardTitle, CardDescription)
- @/components/ui/button (Button)
- @/components/ui/checkbox (Checkbox)
- @/components/ui/radio-group (RadioGroup, RadioGroupItem)
- @/components/ui/badge (Badge)
- @/components/ui/tooltip (Tooltip, TooltipContent, TooltipProvider, TooltipTrigger)
- @/components/ui/separator (Separator)
- cn() from @/lib/utils
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create onboarding layout, page, wizard state machine, and step containers</name>
  <files>
    src/app/onboarding/layout.tsx,
    src/app/onboarding/page.tsx,
    src/components/onboarding/OnboardingWizard.tsx,
    src/components/onboarding/StepOne.tsx,
    src/components/onboarding/StepTwo.tsx
  </files>
  <read_first>
    src/lib/onboarding.ts,
    src/lib/onboarding-db.ts,
    src/app/api/onboarding/route.ts,
    src/app/onboarding/theme.css,
    src/components/ui/card.tsx,
    src/components/ui/button.tsx,
    src/components/ui/progress.tsx
  </read_first>
  <action>
    **1. src/app/onboarding/layout.tsx (per D-16, D-17):**
    - Import `./theme.css` at the top.
    - Export default function `OnboardingLayout({ children })`.
    - Return: `<div className="onboarding-theme dark min-h-screen bg-background text-foreground">` wrapping `{children}`.
    - This scopes the tweakcn CSS variables to this page only and enables dark mode.
    - Do NOT import or modify globals.css.

    **2. src/app/onboarding/page.tsx:**
    - Server component (no 'use client').
    - Import and render `<OnboardingWizard />` from `@/components/onboarding/OnboardingWizard`.
    - Centered layout: `<main className="mx-auto max-w-2xl px-4 py-8">`.

    **3. src/components/onboarding/OnboardingWizard.tsx (per D-19, D-21, D-08, D-03):**
    - `'use client'` component.
    - State: `currentStep: 1 | 2`, `answers: Record<string, unknown>`, `loading: boolean`, `saving: boolean`.
    - On mount (`useEffect`): call `GET /api/onboarding` to load existing snapshot. Populate `answers` from `snapshot.answersByKey` (map each to its `.value`). If `snapshot.completed`, call `router.push('/')` immediately.
    - Import `ONBOARDING_STEP_META`, `getOnboardingQuestionsByStep` from `@/lib/onboarding`.
    - Render:
      - `<Progress value={currentStep === 1 ? 50 : 100} className="mb-6" />` (from shadcn)
      - Step indicator text: `Step ${currentStep} of 2 - ${ONBOARDING_STEP_META[currentStep].title}`
      - If `currentStep === 1`: render `<StepOne questions={step1Questions} answers={answers} setAnswers={setAnswers} />`
      - If `currentStep === 2`: render `<StepTwo questions={step2Questions} answers={answers} setAnswers={setAnswers} />`
      - Navigation buttons:
        - Step 1: `<Button onClick={handleNext}>Next</Button>`
        - Step 2: `<Button variant="outline" onClick={() => setCurrentStep(1)}>Back</Button>` and `<Button onClick={handleComplete} disabled={saving}>Complete</Button>`
    - `handleNext`: POST `/api/onboarding` with `{ currentStep: 1, answers: Object.entries(answers).map(([k,v]) => ({questionKey: k, value: v})) }`, then `setCurrentStep(2)`.
    - `handleComplete` (per D-08): POST `/api/onboarding` with `{ currentStep: 2, complete: true, answers: ... }`, then `router.push('/')`.
    - Loading state: show a spinner/skeleton while initial GET is in flight.

    **4. src/components/onboarding/StepOne.tsx:**
    - Receives `questions: OnboardingQuestion[], answers: Record<string, unknown>, setAnswers: setter`.
    - Renders each question in a `<Card>` with `<CardHeader>` (title + description) and `<CardContent>`.
    - For each question, render the appropriate renderer based on `question.type`:
      - `multi_select` -> `<QuestionMultiSelect>`
      - `single_select` -> `<QuestionSingleSelect>`
    - All step 1 questions (work_areas, career_levels, role_types, regions, work_style) shown on one page within cards.
    - Import question renderers from `@/components/onboarding/` -- they will be created in Task 2.

    **5. src/components/onboarding/StepTwo.tsx:**
    - Same pattern as StepOne but for step 2 questions.
    - Renders: `single_select`, `multi_select`, `file`, and `text` type renderers.
    - For `resume_upload` and `linkedin_pdf`: render `<QuestionFileUpload>`.
    - For `extra_notes`: render `<QuestionText>`.
    - For `linkedin_pdf` specifically: pass a prop `showInfoTooltip={true}` to QuestionFileUpload.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const checks=[['src/app/onboarding/layout.tsx','onboarding-theme'],['src/app/onboarding/page.tsx','OnboardingWizard'],['src/components/onboarding/OnboardingWizard.tsx','use client'],['src/components/onboarding/OnboardingWizard.tsx','api/onboarding'],['src/components/onboarding/OnboardingWizard.tsx','complete: true'],['src/components/onboarding/StepOne.tsx','QuestionMultiSelect'],['src/components/onboarding/StepTwo.tsx','QuestionFileUpload']]; const fails=checks.filter(([f,s])=>!fs.readFileSync(f,'utf8').includes(s)); if(fails.length){console.log('FAIL:',fails.map(([f,s])=>f+' missing '+s).join('; ')); process.exit(1)} console.log('PASS: All 7 structural checks OK')"</automated>
  </verify>
  <done>
    Onboarding layout with scoped theme, page entry point, wizard with step state machine, StepOne and StepTwo containers that delegate to question renderers.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create question renderer components (multi-select, single-select, file upload, text)</name>
  <files>
    src/components/onboarding/QuestionMultiSelect.tsx,
    src/components/onboarding/QuestionSingleSelect.tsx,
    src/components/onboarding/QuestionFileUpload.tsx,
    src/components/onboarding/QuestionText.tsx
  </files>
  <read_first>
    src/app/api/upload-resume/route.ts,
    src/app/api/upload-linkedin/route.ts,
    src/components/ui/checkbox.tsx,
    src/components/ui/radio-group.tsx,
    src/components/ui/tooltip.tsx
  </read_first>
  <action>
    **1. src/components/onboarding/QuestionMultiSelect.tsx:**
    - Props: `question: OnboardingQuestion, value: string[], onChange: (val: string[]) => void`.
    - Render each option as a shadcn `<Checkbox>` with label.
    - Toggling a checkbox adds/removes the option value from the string array.
    - Show `helperText` if present.

    **2. src/components/onboarding/QuestionSingleSelect.tsx:**
    - Props: `question: OnboardingQuestion, value: string | null, onChange: (val: string) => void`.
    - Use shadcn `<RadioGroup>` with `<RadioGroupItem>` for each option.

    **3. src/components/onboarding/QuestionFileUpload.tsx (per D-10, D-11, D-12, D-13):**
    - Props: `question: OnboardingQuestion, value: {resumeId: string, filename: string} | null, onChange: (val) => void, showInfoTooltip?: boolean`.
    - Render a file input: `<input type="file" accept="application/pdf" />`.
    - On file select: validate `file.type === 'application/pdf'` and `file.size <= 10 * 1024 * 1024` (10MB per D-13).
    - Upload via FormData POST to:
      - `/api/upload-resume` if `question.key === 'resume_upload'`
      - `/api/upload-linkedin` if `question.key === 'linkedin_pdf'`
    - IMPORTANT: The `/api/upload-linkedin` route ALREADY EXISTS. Its response shape is `{ success, profile: { id, filename } }` (NOT `{ resume: { id, filename } }` like upload-resume). Handle both shapes:
      ```typescript
      const data = await res.json();
      const id = data.resume?.id || data.profile?.id;
      const filename = file.name;
      onChange({ resumeId: id, filename });
      ```
    - Display uploaded filename with a badge after successful upload. Allow re-upload by clicking again.
    - If `showInfoTooltip` is true (linkedin_pdf per D-12), render a shadcn `<Tooltip>` with an `<Info />` icon (from lucide-react) that shows:
      ```
      How to download your LinkedIn profile as PDF:
      1. Click the Me icon at the top of your LinkedIn homepage.
      2. Select View Profile.
      3. Click the More button (in the introduction section below your name/headline).
      4. Select Save to PDF from the dropdown menu.
      5. The file will automatically download to your computer.
      ```
    - Show error message if file is not PDF or exceeds 10MB.
    - If question is not required (per D-22), show a "Skip" text or simply allow empty state.

    **4. src/components/onboarding/QuestionText.tsx:**
    - Props: `question: OnboardingQuestion, value: string, onChange: (val: string) => void`.
    - Render a `<textarea>` with `placeholder` from question.
    - Show `helperText` if present.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const checks=[['src/components/onboarding/QuestionMultiSelect.tsx','Checkbox'],['src/components/onboarding/QuestionSingleSelect.tsx','RadioGroup'],['src/components/onboarding/QuestionFileUpload.tsx','application/pdf'],['src/components/onboarding/QuestionFileUpload.tsx','10 * 1024 * 1024'],['src/components/onboarding/QuestionFileUpload.tsx','upload-linkedin'],['src/components/onboarding/QuestionFileUpload.tsx','Click the Me icon'],['src/components/onboarding/QuestionText.tsx','textarea']]; const fails=checks.filter(([f,s])=>!fs.readFileSync(f,'utf8').includes(s)); if(fails.length){console.log('FAIL:',fails.map(([f,s])=>f+' missing '+s).join('; ')); process.exit(1)} console.log('PASS: All 7 renderer checks OK'); " && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>
    All 4 question renderer components created. MultiSelect uses Checkbox, SingleSelect uses RadioGroup, FileUpload validates PDF/10MB and posts to correct endpoint (upload-resume or upload-linkedin), Text renders textarea. LinkedIn tooltip shows 5-step instructions.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify onboarding wizard UI end-to-end</name>
  <files>src/app/onboarding/page.tsx</files>
  <action>
    Human verification of the onboarding wizard UI. Run `npm run dev` and test the full wizard flow at http://localhost:3000/onboarding.

    Steps to verify:
    1. Visit http://localhost:3000/onboarding
    2. Verify dark-themed wizard renders with progress bar at top
    3. Step 1: answer questions (multi-select checkboxes, single-select radios)
    4. Click "Next" -- verify progress bar updates and step 2 loads
    5. Click "Back" -- verify step 1 answers are preserved
    6. Click "Next" again to return to step 2
    7. Upload a PDF resume -- verify filename badge appears
    8. Hover the info icon on LinkedIn PDF question -- verify 5-step tooltip
    9. Fill in extra notes text area
    10. Click "Complete" -- verify redirect to / (dashboard)
    11. Visit http://localhost:3000/ and verify dashboard theme is NOT affected
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>User has verified: dark themed wizard renders, step navigation works, file upload accepts PDF, LinkedIn tooltip shows instructions, Complete redirects to /.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Visiting /onboarding shows the wizard UI
- Theme is scoped (dashboard at / is unaffected)
- All 11 questions render correctly across 2 steps
- File upload works for PDF files, posts to correct API (upload-resume or upload-linkedin)
- Complete button saves and redirects
</verification>

<success_criteria>
- /onboarding page loads with dark-themed shadcn UI
- 2-step wizard with progress bar, Next/Back/Complete navigation
- All question types render (multi_select, single_select, file, text)
- File upload validates PDF only, 10MB max, shows filename
- LinkedIn PDF tooltip with 5-step download instructions
- QuestionFileUpload posts to /api/upload-linkedin for linkedin_pdf question (route already exists)
- Completion POSTs to /api/onboarding with complete:true and redirects to /
- Dashboard theme is not affected by onboarding theme
</success_criteria>

<output>
After completion, create `.planning/phases/04-implement-onboarding-page-with-job-recommendation-using-shadcn-ui/04-02-SUMMARY.md`
</output>
